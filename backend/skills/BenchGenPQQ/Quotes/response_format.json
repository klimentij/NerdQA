{
  "type": "json_schema",
  "json_schema": {
    "name": "quote_synthesis_identification",
    "schema": {
      "type": "object",
      "properties": {
        "reflection": {
          "type": "string",
          "description": "A brief reflection on the paper and potential synthesis candidates."
        },
        "quotes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "An array of exact quotes demonstrating novel synthesis, with unified citation formatting. Empty if no suitable quotes are found."
        },
        "references": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "An array of shortened citations corresponding to the numbered references in the quotes. Empty if no quotes are found."
        }
      },
      "required": ["reflection", "quotes", "references"],
      "additionalProperties": false
    },
    "strict": true
  }
}