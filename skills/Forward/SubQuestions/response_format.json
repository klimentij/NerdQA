{
  "type": "json_schema",
  "json_schema": {
    "name": "subquestion_analysis",
    "schema": {
      "type": "object",
      "properties": {
        "reflection": {
          "type": "string",
          "description": "A paragraph containing reflection on the original question, subquestion tree, current subquestion, and search results"
        },
        "queries": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "An array of generated query strings"
        }
      },
      "required": ["reflection", "queries"],
      "additionalProperties": false
    },
    "strict": true
  }
}